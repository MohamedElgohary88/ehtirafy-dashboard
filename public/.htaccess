<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /ehtirafy-dashboard/

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule . /ehtirafy-dashboard/index.html [L]
</IfModule>
